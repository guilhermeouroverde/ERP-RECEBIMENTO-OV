'use strict';
const fs=require('node:fs');const path=require('node:path');const {SupabaseStore}=require('../lib/store');
(async()=>{const s=await new SupabaseStore().snapshot(true);const dir=path.resolve('backups/online_'+new Date().toISOString().replace(/[:.]/g,'-'));fs.mkdirSync(dir,{recursive:true});fs.writeFileSync(path.join(dir,'ouro_verde.json'),JSON.stringify(s.main,null,2));fs.writeFileSync(path.join(dir,'auditoria.json'),JSON.stringify(s.audit,null,2));fs.writeFileSync(path.join(dir,'manifesto.json'),JSON.stringify({at:new Date().toISOString(),main_revision:s.main_revision,audit_revision:s.audit_revision,credentials_included:false},null,2));console.log('Backup operacional salvo LOCALMENTE: '+dir+'\nEste backup nao inclui senhas nem credenciais; mantenha tambem backup do projeto Supabase.');})().catch(e=>{console.error(e.message);process.exitCode=1;});
